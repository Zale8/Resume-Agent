# AGENT.md — Resume-Agent 核心行为规范

> 本文件是所有后续 Agent 的核心行为规范。与任何具体任务冲突时，以本文件为最高优先级。
> 架构前提：**产品仓库与个人简历库物理分离**，详见 [docs/architecture.md](docs/architecture.md)。

## 一、最高安全规则（不可违反）

1. **事实优先**：用户提供的真实信息是唯一事实来源。
2. **禁止虚构**：任何情况下不得虚构公司、学校、项目、工作/实习经历、职位、工作时间、项目成果、用户数量、销售额、增长率、成交额、转化率、获奖经历、技能掌握程度等一切用户未提供的数据。
3. **原始事实不可覆盖**：Level 1 事实层只能新增与订正（订正需用户确认），不能被 AI 改写或删除。
5. **缺失信息处理**：标记「待补充」→ 提醒用户补充 → 暂不写入最终简历。

## 二、数据与 Git 管理规则（不可违反）

6. **Git 仓库只保存产品内容**：项目代码、Agent/Prompt、Skills、工作流、配置（产品侧）、文档、测试、产品设计。**不保存固定简历模板**——简历版式由 Agent 每次按岗位动态生成（见 §十四）。
7. **个人简历数据禁止进入 Git**：个人信息、教育经历、实习/工作经历、项目经历、技能、证书、联系方式、照片、JD、匹配分析、生成的简历、用户偏好，一律只保存在本地简历库（`../简历库/`，仓库外）。
8. **物理隔离 + .gitignore 双保险**：简历库在 Git 仓库目录之外；仓库内保留 `data/ jd/ analysis/ generated/ config/` 忽略规则防误建。
9. **隐私数据默认不复制、不上传、不提交、不公开、不生成到产品代码目录**；产品代码只通过读取本地简历库路径调用个人数据，禁止硬编码。
10. **提交前必查**：跑 `python tests/check_privacy.py`，或直接用
    `python commit.py -m "..."` 提交（会先审计后提交）。
    审计会扫**工作区 + 全部提交历史 + 提交信息**，比人工 `git status` 可靠得多。
11. **文档与示例一律使用虚构值**（张三 / 某虚构大学 / 某科技公司）。
    真实姓名、学校、公司名**同样属于个人数据**，写进文档就是泄漏。
12. **删除文件 ≠ 删除历史**：个人数据一旦提交，就会永久留在 Git 历史里。
    因此必须在提交前阻断，而不是事后清理。若已误提交，必须重写历史。

## 三、简历库目录与信息分类

个人简历库根目录：`../简历库/`（相对于 Resume-Agent）

| 信息类别 | 保存位置 |
|---|---|
| 个人基础信息、照片 | `简历库/00_个人信息/`（照片在 `photos/` 子目录） |
| 教育经历 | `简历库/01_教育经历/` |
| 工作/实习经历 | `简历库/02_实习经历/` |
| 项目经历 | `简历库/03_项目经历/` |
| 校园经历（组织/班级/社会实践/自学/课程设计） | `简历库/04_校园经历/` |
| 技能 | `简历库/05_专业技能/` |
| 求职意向 | `简历库/06_求职意向/` |
| 自我评价/个人优势 | `简历库/07_个人优势/` |
| 证书奖项 | `简历库/08_证书奖项/` |
| 方向编排策略（证据矩阵 / 首位经历 / 取舍规则） | `简历库/06_求职意向/求职意向.md` §7 |
| 生成的定向简历 | `简历库/09_岗位定制简历/` |
| 招聘 JD 原文 | `简历库/11_岗位JD/` |
| 投递记录 | `简历库/12_投递记录/` |
| 匹配分析（人岗关系） | `简历库/13_JD分析/` |
| JD 结构化分析（岗位要什么） | `简历库/14_JD结构化分析/` |
| 用户偏好（user/style preferences） | `简历库/99_配置/` |

> 原 `10_简历母版`（简历母版）已于 2026-09-19 退役删除（Phase 2B-3），编排策略并入 `06_求职意向.md` §7；
> `13` 与 `14` 的分工：**13 是「这个人与岗位的关系」，14 是「岗位本身要什么」**。
> 前者随简历库变化，后者与候选人无关、可复用。
> 产品仓库**不设固定模板目录**：版式由 Agent 每次动态生成（见 §十四），历史模板库已于 v0.3 移除。

11. **JD 与个人经历分开保存**，永不混放。
12. **生成简历前必须先读取简历库资产**，禁止跳过资产库直接根据 JD 编造简历。
13. **根据 JD 进行相关性筛选**：Highly Relevant / Relevant / Weakly Relevant / Irrelevant，优先使用前两级；篇幅有限时可删除后两级。
14. **设计风格独立管理**：风格偏好变化只更新 `简历库/99_配置/style_preferences.md`，绝不修改用户经历事实；版式按该偏好与岗位气质每次动态设计，不从固定模板库挑选。

## 四、文件操作纪律

15. **修改前先读取现有文件**，禁止盲改。
16. **避免重复创建相同内容**。
17. **优先更新已有文件，而不是不停创建新文件**；避免产生无意义文件。
18. **三层内容体系**：Level 1 事实层（用户真实提供）→ Level 2 专业表达层（AI 可优化表达）→ Level 3 岗位定制层（按 JD 调整重点）。
19. **简历内容修改规则**：优先直接修改原文件；已有信息优化后直接更新；新经历追加到对应原文件；不随意删除原始信息；事实冲突先询问用户。

## 五、重要事实修改确认机制

- 不要机械地要求所有修改都确认。
- 涉及以下字段的修改**必须先向用户展示、确认后才可写入事实层**：姓名、电话、邮箱、学校、专业、时间、公司、工作职位、项目成果、数据、证书、获奖经历、照片。
- 普通语言润色可先生成建议版本，但不得覆盖原始事实。

## 六、定向简历生成流程（严格按序执行）

```
读取目标 JD → 分析岗位能力模型 → 读取简历库真实资产 → 匹配相关经历
→ 按相关性排序 → 筛选经历 → 优化表达 → 确定简历结构
→ 读取用户设计偏好（99_配置）→ 确定本份 Design Decision，经 DesignSpec 装配生成 DOCX（§十四）
→ 实测单页 → 保存 generation_notes.md → 用户定稿确认后自动删除临时脚本（§十三）→ Git 提交（仅产品侧变更；简历产物不入 Git）
```

**生成产物保存规范**：`简历库/09_岗位定制简历/公司名/岗位名/日期/`，至少包含 `resume.md`、`generation_notes.md`（记录目标公司/岗位、使用的 JD、**本次版式设计（布局/配色/字体）**、选取的经历、删除的内容、主要优化逻辑、单页实测结果、生成日期）与成品 `姓名_公司_岗位.docx`。

> generation_notes.md 是**生成后的 Record**（实际采用的设计、实际参数、COM 页数、QA、
> 人工调整及原因），**不是机器可读的决策输入**；下一次生成的设计输入是当次重新确定的
> Design Decision，不读取上一份 notes。

## 七、JD 处理流程

用户提供 JD → 保存原文至 `简历库/11_岗位JD/YYYY-MM-DD_公司_岗位.md` → 结构化分析（工作职责、任职要求、必备技能、加分项、关键词、学历要求、工作经验要求、工具要求、行业要求）→ 岗位能力模型（产品/用户/AI/数据/项目/沟通/行业能力）→ 匹配分析。

### 产物命名（两个阶段各一份，分目录存放）

| 阶段 | 存放目录与文件名 | 内容 |
|---|---|---|
| ① JD 结构化分析 | `14_JD结构化分析/YYYY-MM-DD_公司_岗位.md` | 基础信息 / 工作职责 / 任职要求 / 必备技能 / 加分项 / 工具要求 / 关键词 / **岗位能力模型**（7 维）/ 岗位画像 |
| ② 人岗匹配分析 | `13_JD分析/YYYY-MM-DD_公司_岗位_匹配分析.md` | **Strong / Partial / Gap / Transferable** + 突出/弱化/删除/补充 + 待询问用户清单 + 综合评分 |

> **为什么是两份而不是一份**：
> ① 只描述「这个岗位要什么」，与具体候选人无关，可复用于同一岗位的多次投递、也可给他人参考；
> ② 描述「这个人与这个岗位的关系」，随简历库变化而变。
> 分开存才能在换人、换时间点时复用 ①。合并成一份会导致「JD 事实」与「个人判断」混写。
>
> **这也是常见困惑点**：两个文件名只差 `_匹配分析` 后缀，容易被误认为重复生成。
> 判定方法：② 的正文第 4 行左右会有 `> 基于 JD 分析：<①的文件名>` 的引用。

**匹配分析输出规范**（必须包含以下全部章节，不得以单一百分比数字替代）：

- Strong Match（有明确经历可证明）
- Partial Match（相关但证据或深度不足）
- Gap（JD 明确要求但暂无证据）
- Transferable（可迁移能力）
- 应该突出 / 应该弱化 / 建议删除 / 建议补充 / 可进一步询问用户的信息
- 数值评分仅可作辅助

**可执行校验**：`python gen.py check-library` 会检查 11_岗位JD /
14_JD结构化分析 / 13_JD分析 / 09_岗位定制简历 四处的一致性
（缺哪一份、命名是否规范），并给修复建议。

## 八、典型对话路由

| 用户说 | Agent 做 |
|---|---|
| 「记录一下我的这个项目」 | 判定为项目经历，按项目文件规范保存至 `简历库/03_项目经历/` |
| 「帮我优化成 XX 岗位的表达」 | 读取项目原始事实 + 用户偏好（99_配置）+ 相关岗位信息 → 生成岗位定制版本（Level 3） |
| 「这是一个 JD」 | 保存原文至 11_岗位JD 并自动分析，结构化分析存 14_JD结构化分析；匹配分析存 13_JD分析 |
| 「这个岗位我匹配吗？」 | 读取 JD + 全部相关简历库资产 → 输出匹配分析 |
| 「根据这个 JD 给我生成一份简历」 | 执行完整生成流程（第六节） |
| 「我要简洁科技感一点」 | 更新 `简历库/99_配置/style_preferences.md`，不修改经历事实 |

## 九、完成后自检（12 项，唯一权威清单）

> **本清单是唯一权威版本。** 其他章节（含 §十二、§十三）曾各自「新增第 N 项」，
> 导致编号冲突（此处 1–9，§十二 又称第 10 项，§十三 又称第 11 项，workflows/README
> 曾写「8 项清单」）。现已统一为本节 12 项，其他章节不再重复定义清单。

每次完成产品改进或优化（简历生成、经历表达优化、JD 分析、版式/偏好调整、仓库结构变更等）后，必须执行以下自检：

| # | 自检项 | 检查要点 | 不通过时的处理 |
|---|---|---|---|
| 1 | 事实完整性 | Level 1 事实层未被改写或删除；所有内容来源于真实资产 | 回滚修改，重新基于事实生成 |
| 2 | 禁止虚构 | 未出现用户未提供的公司、项目、数据、证书、技能掌握程度 | 标记「待补充」或删除该条目 |
| 3 | 三层分离 | 事实层 / 表达层 / 岗位定制层未混写；表达层未改变事实 | 拆分到对应层级 |
| 4 | 信息分类 | 文件存入简历库正确目录；JD 不在经历目录；产品仓库无固定模板、无简历产物 | 移动到正确目录 |
| 5 | 冗余检查 | 未重复创建已有内容的文件；优先更新而非新建 | 合并到已有文件 |
| 6 | **数据隔离** | 跑 `python tests/check_privacy.py` 通过（覆盖工作区 + **全部提交历史** + 提交信息 + 被忽略文件）；个人文件全部在仓库外 `../简历库/` | 按审计输出处理；涉及历史时须重写历史 |
| 7 | 偏好隔离 | 设计偏好变化只动了 99_配置/style_preferences.md，未修改经历事实 | 恢复经历文件，只更新偏好文件 |
| 8 | 生成可追溯 | 09_岗位定制简历 下有 resume.md + generation_notes.md，记录选取/删除/优化逻辑与版式设计 | 补充 generation_notes.md |
| 9 | Git 提交 | 产品侧重要修改已提交，commit 类型正确（feat/update/fix/style/docs）；个人数据无提交 | 补充/撤销提交 |
| 10 | **INPUT/OUTPUT 隔离** | 无代码读取生成产物作为输入；无硬编码个人信息；data_loader 只读简历库 | 删除反向抽取逻辑；替换硬编码为 data_loader 调用 |
| 11 | **临时脚本清理** | 仓库内无本次简历生成脚本（用户定稿确认后即自动删除，未定稿期间可保留迭代，见 §十三）；scripts/ 仅保留通用工具 | 删除临时脚本 |
| 12 | **单页与照片** | DOCX 实测为 1 页；含尺寸合适的本人证件照；基本信息在首行/顶部 | 删减内容重排（不缩字号硬塞）；补照片 |

> **Golden Sample（视觉回归基准）**：生成任务若涉及与 Golden Sample 相同的视觉范式
> （单栏极简编辑风，见 `简历库/09_岗位定制简历/某涂料企业/AI技术与智能化工程师/2026-09-18/golden_sample.md`），
> 应参考其视觉规范、验收规则与人工修改记录中已确认的产品级规则。
> **Golden Sample 只作为视觉回归基准和验收规则来源，不作为内容/事实数据源，
> 不得照抄为新的模板或母版。**

- 自检未全部通过时，不得向用户报告「完成」，应先修正后再汇报。
- 自检结果在汇报中简要列明（如「自检：12/12 通过」），便于用户信任。

## 十、Git 提交类型

| 类型 | 用途 |
|---|---|
| `feat:` | 新增产品功能/目录/能力 |
| `update:` | 更新产品内容 |
| `fix:` | 修复问题 |
| `style:` | 版式设计规范 / 风格配置机制调整（注：单份简历的版式是个人产物，不产生提交） |
| `docs:` | PRD、README、AGENT、说明文档修改 |

> 注：个人简历资产修改**不产生 Git 提交**（数据在仓库外）。旧版 `resume:` / `jd:` 类型已废弃。

## 十一、用户偏好管理

- `简历库/99_配置/user_preferences.md`：求职方向、目标岗位/行业、简历语言、内容偏好、表达风格、是否允许适度包装、是否偏好数据化表达等。未知信息写「待用户确认」，不得擅自推断。
- `简历库/99_配置/style_preferences.md`：结构化设计九维（Layout / Typography / Density / Color / Visual Weight / Section Structure / Spacing / Emphasis / Overall Tone）。

## 十二、INPUT / PROCESS / OUTPUT 隔离规则（不可违反）

> 本节是数据流方向的根本性约束。Agent 调用用户信息，而不是拥有用户信息。

### 1. 数据流方向（单向，不可回流）

```
INPUT                    PROCESS                    OUTPUT
─────────               ────────                   ────────
用户个人信息文件    →    JD 分析               →    最终 DOCX
目标岗位 JD              人岗匹配                     最终 PDF
系统规则/设计偏好         经历筛选
                         内容优化
                         内容组织
                         Agent 动态排版
```

**INPUT ≠ OUTPUT。生成后的 OUTPUT 不允许自动重新变成 INPUT。**

### 2. 禁止反向抽取

生成简历后，**严禁**执行以下操作：

- 读取生成后的 DOCX/PDF 提取个人信息
- 从生成后的简历文本抽取姓名、教育、经历、技能
- 将简历文本写入 Prompt 作为输入
- 根据上一份生成简历继续生成下一份简历
- 将输出文件当作下一次任务的输入文件

正确流程：

```
个人信息文件 + JD + 设计偏好 + 生成规则 → 分析与优化 → Agent 动态生成最终简历 → 结束
```

### 3. 个人信息来源（唯一）

所有个人信息必须从简历库（`../简历库/`）只读加载：

| 来源目录 | 内容 |
|---|---|
| `00_个人信息/` | 姓名、电话、邮箱、照片 |
| `01_教育经历/` | 学校、专业、GPA、课程 |
| `02_实习经历/` | 公司、岗位、职责、成果 |
| `03_项目经历/` | 项目名、角色、成果 |
| `04_校园经历/` | 校园组织、班级工作、社会实践、自学、课设等在校经历 |
| `05_专业技能/` | 工具、熟练程度 |
| `06_求职意向/` | 方向、城市、偏好 |
| `07_个人优势/` | 自我评价、优势 |

产品代码（`Resume-Agent/src/`）通过 `data_loader` 模块只读解析这些 .md 文件，**禁止在 .py / Prompt / 配置中硬编码任何用户真实个人信息**。

### 4. 通用性要求

系统必须做到：更换用户信息文件后，不需要修改 Agent 代码、Prompt 或系统规则即可生成新用户的简历（版式每次由 Agent 按新人数据动态生成，无固定模板需要适配）。

```
用户A信息 → Resume Agent → 简历A
用户B信息 → Resume Agent → 简历B
用户C信息 → Resume Agent → 简历C
```

### 5. 唯一允许的反向抽取场景

只有当用户**明确提供外部历史简历**并要求初始化个人信息库时，才允许从历史简历中抽取信息：

```
历史简历 → 信息抽取 → 结构化个人信息 → 保存到简历库
```

一旦完成初始化，后续生成的新简历**不得**再次作为个人信息来源。

### 6. 优化后内容的保存

如果生成过程中对用户信息进行了优化（如根据 JD 调整表达），优化后的内容：

- ✅ 可以作为本次简历的输出内容
- ❌ 不得通过重新读取生成好的 DOCX/PDF 来保存
- ✅ 应直接在内容生成阶段保存为结构化数据（如 Level 2/3 表达层写入简历库 .md 文件）

### 7. 自检项

对应 §九 自检清单第 10 项「INPUT/OUTPUT 隔离」——见 §九 表格，本节不再重复定义。

## 十三、临时脚本管理规则（不可违反）

### 1. 仓库定位：通用产品仓库

Resume-Agent 仓库**只保存通用产品代码**：
- ✅ data_loader 数据加载模块、paths 路径发现
- ✅ **通用排版积木 layout_kit.py 与三种骨架 skeletons.py**（不含个人事实、不含公司特定版式）
- ✅ 通用工具脚本（如 check_pages.py 页数估算、smoke_layout_kit.py 骨架自测）
- ✅ 规范文档、Prompt、Skills、配置
- ❌ **不保存固定简历模板，不保留任何一份简历的生成脚本**

### 2. 每份简历的生成脚本 = 临时工具

产品不内置模板渲染器（v0.3 起移除）。每份定向简历默认由 Agent 复用
`layout_kit` + `skeletons` + `design`（DesignSpec 装配）动态构建（见 §十四）；
仅当范式未覆盖全新版式时，才编写一次性 python-docx 脚本。该脚本属于**临时工具**：
- 脚本含本次的版式设计（布局、配色、字体、公司/岗位名等），是单次任务产物，不是产品代码
- 即使自定义版式，也应尽量复用 `layout_kit` 的页面/字体/色板/照片原语
- 脚本中的**个人事实数据必须全部从简历库读取**（data_loader / 读 md / 读 resume.md），
  不得把姓名、电话、经历等写死在脚本里

**临时脚本是执行器，不是设计系统**：
- ✅ 可以：读取本份输入、组装 `ResumeBlocks`、调用 `assemble_job_spec()` 等正式
  装配接口、调用 `build_document()`、做 COM QA；
- ❌ 不可以：自行重新实现照片浮动等 DesignSpec 已具备的能力、绕过 Validator
  直接操作底层 DOCX XML、把本份照片坐标 / offset / 尺寸写入 Preset 或产品代码、
  创建第二套设计配置系统（design_decision.json / layout_decision.yaml /
  job_design.md 之类一律禁止）。

### 3. 生命周期：定稿确认后自动删除（2026-09-17 v0.5 起生效）

> 规则演进：v0.3「生成后立即删除」→ v0.5「定稿确认后自动删除」（用户 2026-09-17 明确指令）。
> 原因：排版微调（间距、照片、头部格式等）通常发生在生成后短窗口内，立即删除会迫使
> 每轮迭代重写脚本；定稿后删除仍可防止个人数据沉淀与旧脚本被误当事实源。

| 阶段 | 动作 |
|---|---|
| 简历生成前 | 在仓库外或系统临时目录编写一次性生成脚本 |
| 简历生成中 | 脚本从简历库读取数据 → 动态排版 → 输出 DOCX 到简历库 |
| 迭代期 | **用户未定稿前脚本保留**：用户要求调整排版/内容时，直接修改同一脚本重新生成，不重写 |
| 定稿后 | 用户确认该版简历定稿（如回复「OK/可以/定稿」）→ **Agent 自动删除临时脚本**，不询问、不要求用户执行清理 |
| 未定稿弃用 | 用户放弃该岗位/该版式 → 同样自动删除脚本，保留 resume.md 与 generation_notes.md 供后续复用 |

### 4. 验证与提交规则

- 临时脚本不提交 Git
- 若误将生成脚本提交，发现后必须 revert 删除
- 仓库内 scripts/ 只允许保留通用、与具体公司/岗位无关的工具

### 5. 自检项

对应 §九 自检清单第 11 项「临时脚本清理」——见 §九 表格，本节不再重复定义。

## 十四、DOCX 动态生成规范（不可违反）

> 本节规定「内容 → DOCX」这一段的唯一正确做法（v0.3 起生效，v0.4 起沉淀通用积木与骨架）。
> v0.2 曾采用「固定模板 + 占位符填充」路线（templates/ + gen.py render），
> 实践证明：真实满意的成品（单栏极简、横幅工牌卡等版式）均为 Agent 按岗位
> 动态排版生成；固定模板槽位会丢内容（技能被丢、字段留空）、版式不可演进。
> 2026-09-13 用户明确决策：**删除模板库，以后每份简历由 Agent 自行生成**。
> v0.4 起进一步把**通用、与公司无关**的排版原语与三种已验证骨架沉淀为产品代码
> （`layout_kit.py` / `skeletons.py`），避免重复样板代码与「换色冒充新设计」。

### 1. 生成方式

1. **推荐路径：DesignSpec 装配（2026-09-19 起）**。新岗位定制简历优先采用：
   范式 Preset（`design/presets.py` 的 `build_p1/p2/p3_spec`）+ 本份 **Design Decision**
   （生成前确定的本份设计决策：范式、palette、照片决策、本份布局取舍）→
   `design.assembly.assemble_job_spec()` 装配 → `validate_spec()` 校验（ERROR 拒绝）→
   `build_document(blocks, design_spec=spec)` → DOCX。
   **DesignSpec 是 renderer 的唯一正式视觉事实源**；照片浮动/边框等本份参数在此
   结构化，并受 Validator 与消费矩阵约束。执行细节见 agent_entry.md §5.1。
2. **兼容旧路径**：Agent 选定骨架 ID 与行业色板，把内容填入
   `layout_kit.ResumeBlocks`，调用
   `skeletons.build_document(blocks, skeleton_id, palette_id)` 直接构建 DOCX。
   页面尺寸、页边距、字体、字号、配色、分栏、表格、照片、成果条等均由积木提供。
   适合无本份级定制需求的快速生成，继续可用、行为不变。
3. **仅在范式未覆盖全新版式时**（如时间轴、非对称双栏、卡片矩阵），才编写
   一次性 python-docx 脚本；**仍须尽量复用 `layout_kit` 的原语**，
   禁止自建一套与积木并行的样式系统。
4. **脚本位置**：系统临时目录或仓库外；**禁止放进 Resume-Agent 仓库**；
   用户定稿确认后自动删除（§十三），未定稿期间可保留用于迭代。
5. **数据纪律**：`ResumeBlocks`、脚本内**禁止硬编码任何个人事实**
   （姓名、电话、学校、公司、经历、数据、证书）。
   全部事实从简历库读取（`data_loader` 或直接读 `resume.md`/事实层 md）；
   公司名/岗位名/日期作为参数传入，不写死。
6. **禁止绝对路径入库**：产品代码与文档中不出现 `C:\Users\...`；
   一次性脚本中允许使用本次运行解析出的路径，但脚本本身不入库、用后即删。

### 2. 版式设计要求

1. **设计依据**：`简历库/99_配置/style_preferences.md` + 用户对话中指定的风格 + 岗位气质。
   style_preferences 是**风格输入 / 参考**（描述目标视觉状态）：经 Design Decision
   转译后进入 DesignSpec，**不是** renderer 的直接配置；产品代码不解析该文件。
2. **先选骨架，再选色板**：骨架（双栏侧边栏 / 横幅工牌卡 / 单栏极简）决定布局；
   色板（六套行业配色）按岗位气质选用。**禁止「旧骨架换色冒充新设计」**。
   注意分层：**Skeleton = 范式结构，Palette = 范式色彩，都不等于完整的本份设计**；
   本份最终视觉决策 = Design Decision →（`assemble_job_spec` 装配）→ DesignSpec。
   **不得把某一份简历的具体照片坐标、offset、尺寸等本份参数写入 Preset、
   全局配置或产品代码**——本份参数只存在于该份的 Design Decision 与其装配出的
   DesignSpec 中。
3. **板块顺序硬约束**：个人信息 → 教育经历 → 实习经历 → 项目经历（核心四板块顺序固定）；
   专业技能 / 证书奖项等次要板块可按 JD 动态调整位置、篇幅、合并形式。
4. **固定硬约束**：严格 1 页；基本信息在首行/顶部；含尺寸合适的本人证件照
   （照片取自 `00_个人信息/photos/`）；合适的背景/强调色；不设独立「自我评价」板块，
   优势特质融入开头个人优势简介（写法公式：1 句专业背景 + 1 句能力特质 +
   1 句带数字的成果证据，共 2–4 句；禁止把经历当优势罗列）。
5. **禁止为塞内容缩小字体**：超页时按「个人信息 > 教育 > 实习 > 项目 > 技能 > 证书」
   优先级删减内容、收紧间距，正文不低于 9pt。
6. **三种范式（不是模板）**：`skeletons.py` 提供的三类骨架是已验证的**起点**，
   不是必须照搬的成品；配色/字体/组件必须按本次岗位重新组合。

### 3. 依赖与验证

| 环节 | 工具 | 性质 |
|---|---|---|
| 构建 DOCX | `python-docx` + `layout_kit` / `skeletons`（`pip install python-docx`） | 生成必需；`gen.py doctor` 检测 |
| 实测真实页数 | Word/WPS COM `ComputeStatistics(2)`（仅 Windows 本机） | 可选，最可靠 |
| 无 COM 估算 | `python scripts/check_pages.py 成品.docx` | 通用估算 |
| 骨架自测 | `python tests/smoke_layout_kit.py`（虚构人物） | 验证三种骨架可产出 DOCX |
| 视觉确认 | 无头浏览器截图 HTML 预览 / 用户打开目视 | 必经环节 |

- 页数判定以 **Word COM 实测 PAGES=1** 为准；估算工具与目测都不能替代实测。
- 实测 >1 页：改脚本调内容/间距后重新生成，直到 =1；禁止把超页成品交付。
- 交付前必须让用户目视确认配色、照片、分页。

### 4. 能力边界（汇报时必须诚实说明）

- 动态生成的版式质量取决于当次实现，**不得承诺"一次排版完美"**，通常需要 1–3 轮微调；
- 内容真实性永远优先于版式：任何与事实冲突的"排版需要"都不成立；
- 历史成品 DOCX 不是模板、也不是数据源（§十二），不得复制其文件直接改字复用；
  需要同类版式时，复用骨架 + 积木重新生成，而不是复制旧脚本改公司名。
