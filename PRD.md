# PRD — Resume Agent

| 版本 | 日期 | 状态 |
|---|---|---|
| MVP v0.1 | 2026-09-08 | 已确认 |

## 1. 产品背景

求职者在投递不同岗位时，普遍需要针对每个 JD 手工改写简历。这个过程中存在三类典型痛点：

- 真实经历散落在聊天记录、旧简历中，没有沉淀为可复用的资产；
- 每次改简历都近乎「重写」，表达质量不稳定，且容易在反复修改中失真；
- 不清楚自己的经历与目标岗位的匹配情况，不知道该突出什么、删掉什么。

## 2. 产品定位

AI 个性化简历管理与生成系统。以「个人职业资产库」为核心：用户的真实经历长期保存、分层管理；AI 基于资产库做表达优化与岗位定向筛选，而不是凭空生成。

## 3. 用户问题

| 用户问题 | Resume-Agent 的解法 |
|---|---|
| 经历没有沉淀 | 本地简历库按分类长期保存（仓库外，不入 Git），Level 1 事实层不可篡改 |
| 改简历 = 每次重写 | Level 2 表达层 / Level 3 岗位定制层，复用与定向结合 |
| 不知道与岗位的差距 | JD 结构化分析 + Strong/Partial/Gap/Transferable 匹配分析 |
| 简历版本混乱 | 每个岗位独立目录 + generation_notes.md；产品规则用 Git 管理，个人数据本地留存 |

## 4. MVP 目标

用户通过 AI 对话完成以下闭环：

录入/维护简历信息 → 经历表达优化 → 自动分类保存 → 输入 JD → 岗位分析 → 资产匹配（优势/差距/可迁移能力）→ 内容筛选与优化 → 按模板与设计风格生成定向简历 → 版本保存 → Git 管理全部历史。

## 5. 核心用户流程

```
个人经历 → 简历资产库 → JD → JD分析 → 经历匹配
→ 内容筛选与优化 → 模板与设计风格 → 定向简历 → Git保存
```

## 6. 简历资产管理

- **三层体系**：
  - Level 1 事实层：用户真正提供过的信息（公司、学校、项目、职位、时间、职责、行动、工具、结果、明确数据），不得被 AI 擅自修改；
  - Level 2 专业表达层：AI 可优化语言表达，不能改变事实；
  - Level 3 岗位定制层：按 JD 调整表达重点，必须来源于真实经历。
- **分类与存储**：个人简历资产保存在仓库外 `../简历库/`（00_个人信息 ~ 14_JD结构化分析 + 99_配置），**不进入 Git**；产品仓库只存规则、模板、工作流。
- **文件规范**：每个重要项目单独一个 .md 文件，包含「基础信息 / 原始事实 / AI优化表达 / 能力标签 / 岗位定制版本 / 修改记录」。
- **缺失信息**：标记「待补充」并提醒用户，禁止编造。

## 7. JD 分析

- JD 原文完整保存至 `简历库/11_岗位JD/`，命名 `YYYY-MM-DD_公司_岗位.md`。
- 结构化分析：工作职责、任职要求、必备技能、加分项、关键词、学历要求、工作经验要求、工具要求、行业要求。
- 岗位能力模型：产品 / 用户 / AI / 数据 / 项目 / 沟通 / 行业能力。
- 结构化分析存 `简历库/14_JD结构化分析/`；匹配分析存 `简历库/13_JD分析/`（均为个人数据，不入 Git）。

## 8. 匹配分析

读取 JD 后搜索整个个人简历资产库，至少输出：

- **Strong Match**：用户已有明确经历可以证明；
- **Partial Match**：存在相关经历，但证据或深度不足；
- **Gap**：JD 明确要求，但用户资料中暂无对应证据；
- **Transferable**：无完全相同经历，但存在可迁移能力；
- **应该突出 / 应该弱化 / 建议删除 / 建议补充 / 可进一步询问用户的信息**。

数值评分仅可作辅助，不能替代具体分析。

## 9. 简历定制

- **相关性分级**：Highly Relevant / Relevant / Weakly Relevant / Irrelevant，优先使用前两级；篇幅有限时可删除后两级。
- **生成流程（不可跳过资产库）**：读 JD → 分析能力模型 → 读真实资产 → 匹配经历 → 相关性排序 → 筛选 → 优化表达 → 确定结构 → 读设计偏好 → 应用模板 → 生成简历 → 保存生成说明。
- **产物**：`简历库/09_岗位定制简历/公司名/岗位名/日期/resume.md` + `generation_notes.md`（个人数据，不入 Git）。

## 10. 设计风格

- 自然语言偏好（如「苹果风、极简、留白多」「投国企要稳重正式」）转换为结构化九维：Layout / Typography / Density / Color / Visual Weight / Section Structure / Spacing / Emphasis / Overall Tone。
- 偏好保存于 `简历库/99_配置/style_preferences.md`（个人数据，不入 Git）；模板本身在产品仓库 `templates/`（入 Git）。
- **风格与内容相互独立**：同一套内容可应用不同模板；同一模板可用于不同岗位。

## 11. 数据与 Git 版本管理

- **产品仓库（Resume-Agent/，Git 追踪）**：代码、Agent/Prompt、Skills、模板、工作流、文档。
- **个人简历库（../简历库/，禁止入库）**：个人信息、经历、技能、照片、JD、分析、生成简历、用户偏好。
- 物理隔离（仓库外）+ .gitignore 双保险；提交前必须确认暂存区无个人真实信息。
- 提交类型：feat / update / fix / style / docs（个人数据变更不产生提交）。
- 产品规则与模板的每次重大修改均需 commit，保证可回溯。

## 12. MVP 成功标准

用户能够通过 AI 对话：

- 新增经历
- 修改经历
- 分类保存
- 输入 JD
- 获得岗位分析
- 获得匹配分析
- 讨论简历设计风格
- 根据 JD 生成针对性简历
- 保存历史版本
- 使用 Git 返回历史状态

以上跑通即认为 MVP v0.1 达成。

## 13. 非目标（MVP 不做）

自动搜索招聘岗位、自动登录招聘网站、自动填写网申、自动投递简历、自动处理验证码、自动发送邮件、招聘网站爬虫、招聘账号管理、自动岗位推荐。

## 14. 后续版本规划

| 版本 | 方向 |
|---|---|
| v0.2 | PDF/DOCX 导出、更多模板、匹配评分体系细化、简历多语言 |
| v1.0 | 网申表单理解与辅助填写（保留人工确认环节） |
| v2.0 | 岗位搜索、自动投递、投递进度跟踪（受平台规则与账号安全约束） |
